= form_for  [:admin, @diet] do |diet_form|
  %fieldset
    %legend Diet Details
    .control-group
      = diet_form.label :name, 'Diet Name'
      .controls
        = diet_form.text_field :name
    .form-actions
      = diet_form.submit 'Save'

