- @wings.each do |wing|      
  .tiles
    %a{:class => "tile icon bg-color-#{wing.color}", href: wing_rooms_path(wing)}
      .tile-content
        %h1.icon-large.icon-home
      .brand
        .name
          = wing.name

-#%img(src="/assets/Google.ico")
