- @rooms.each do |room|      
  .tiles
    %a{:class => "tile icon bg-color-redDark", href: wing_room_patients_path(@wing, room)}
      .tile-content
        %h1.icon-large.icon-bell
      .brand
        .name
          = room.number
