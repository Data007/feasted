.tiles
  %a{:class => "tile icon bg-color-#{@patient.color(@kind)}", href: edit_wing_room_patient_meal_path(wing_id: @wing.id, room_id: @room.id, patient_id: @patient.id, kind: @kind)}
    .tile-content
      %h1.icon-large.icon-basket
    .brand
      .name
        Patient Meal

  %a{:class => "tile icon bg-color-#{@patient.color(@kind)}"}
    .tile-content
      %h1.icon-large.icon-basket
    .brand
      .name
        Family Meal
