.tiles
  %a{:class => "tile icon bg-color-#{@patient.color(@kind)}", href: new_wing_room_patient_meal_order_path(wing_id: @wing.id, room_id: @room.id, patient_id: @patient.id, meal_id: @meal.id)}
    .tile-content
      %h1.icon-large.icon-basket
    .brand
      .name
        Patient Meal

  %a{:class => "tile icon bg-color-#{@patient.color(@kind)}", href: new_wing_room_patient_meal_order_path(wing_id: @wing, room_id: @room, patient_id: @patient, meal_id: @meal, family_member: true)}
    .tile-content
      %h1.icon-large.icon-basket
    .brand
      .name
        Family Meal
